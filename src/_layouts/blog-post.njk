---
layout: base.njk
ogType: article
section: blog
sectionLabel: "Блог"
---
{% set blogSchema = {
  "@context": "https://schema.org",
  "@type": "BlogPosting",
  "headline": title,
  "description": description,
  "datePublished": date,
  "dateModified": date,
  "author": {"@type": "Person", "name": "Сергій Коржов"}
} %}
<script type="application/ld+json">{{ blogSchema | dump | safe }}</script>
<div class="article-wrap">
  <div class="breadcrumb">
    <a href="/">Головна</a><span>/</span><a href="/blog/">Блог</a><span>/</span><span>{{ title }}</span>
  </div>

  <div class="article-header">
    <span class="eyebrow">{% if audience == 'colleagues' %}Для колег{% else %}Особистий журнал{% endif %}</span>
    <h1>{{ title }}</h1>
    <div class="article-meta">
      <div class="author-chip">
        <div class="author-avatar"></div>
        <span>Сергій Коржов</span>
      </div>
      <div class="article-meta-item">
        <svg viewBox="0 0 24 24" fill="none" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4.5" width="18" height="16" rx="2"/><path d="M3 9.5h18M8 3v3M16 3v3"/></svg>
        <span>{{ date | ukDate }}</span>
      </div>
      <div class="article-meta-item">
        <svg viewBox="0 0 24 24" fill="none" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><path d="M12 7v5l3.5 2"/></svg>
        <span>{{ content | readingTime }} хв читання</span>
      </div>
    </div>
  </div>

  <div class="article-body">
    {{ content | safe }}
  </div>
</div>
